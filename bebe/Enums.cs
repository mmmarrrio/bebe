namespace UniversityEF;

public static class Enums
{
    public enum UserRole { Manager = 1, Student = 2 }
    public enum TestType { Education = 1, Activity = 2, Other = 3 }
    public enum AnswerType { Single = 1, Multiple = 2, Text = 3}
}